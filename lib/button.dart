import 'package:flutter/material.dart';

final ButtonStyle signupbutton = ElevatedButton.styleFrom(
  minimumSize: Size(357, 50),
  backgroundColor: Colors.purple[300],
  elevation: 0,
  shape: const RoundedRectangleBorder(
    borderRadius: BorderRadius.all(
      Radius.circular(70),
    ),
  ),
);

final ButtonStyle loginbutton = ElevatedButton.styleFrom(
  minimumSize: Size(357, 50),
  backgroundColor: Colors.black,
  elevation: 0,
  shape: const RoundedRectangleBorder(
    borderRadius: BorderRadius.all(
      Radius.circular(70),
    ),
  ),
);

final ButtonStyle signoutbutton = ElevatedButton.styleFrom(
  minimumSize: Size(257, 40),
  backgroundColor: Colors.black,
  elevation: 0,
  shape: const RoundedRectangleBorder(
    borderRadius: BorderRadius.all(
      Radius.circular(70),
    ),
  ),
);

final ButtonStyle prmg = ElevatedButton.styleFrom(
  minimumSize: Size(287, 40),
  backgroundColor: Colors.black,
  elevation: 0,
  shape: const RoundedRectangleBorder(
    borderRadius: BorderRadius.all(
      Radius.circular(70),
    ),
  ),
);

final ButtonStyle upsc = ElevatedButton.styleFrom(
  minimumSize: Size(100, 40),
  backgroundColor: Colors.black,
  elevation: 0,
  shape: const RoundedRectangleBorder(
    borderRadius: BorderRadius.all(
      Radius.circular(70),
    ),
  ),
);

final ButtonStyle quiz = ElevatedButton.styleFrom(
  minimumSize: Size(130, 40),
  backgroundColor: Colors.black,
  elevation: 0,
  shape: const RoundedRectangleBorder(
    borderRadius: BorderRadius.all(
      Radius.circular(70),
    ),
  ),
);
